# vgururao.github.io — venkateshrao.com

Personal site for Venkatesh Rao. Deployed via GitHub Pages at the custom domain **venkateshrao.com** (set in `CNAME`).

## Architecture

Pure static HTML — no build system, no framework, no package.json. Editing files and pushing to `master` is the entire deploy pipeline. GitHub Pages serves the repo root directly.

```
git push origin master   # deploy
```

## File Map

### Root

| File | Purpose |
|------|---------|
| `index.html` | Main bio page — Consulting, Workshops/Speaking, Bio, Contact |
| `library.html` | Personal library catalog (1.7 MB). **Auto-generated** — do not edit manually. Updated weekly by launchd agent in `vgr-library-code/`. |
| `404.html` | Custom 404 page |
| `CNAME` | Custom domain: `venkateshrao.com` |
| `CV2019.pdf` | CV (last updated 2019) |
| `email.jpeg` | Email address rendered as image (anti-spam) |
| `weick.png` | Karl Weick paper image, embedded in consulting section |
| `headshotVGR{200,400,800}.JPG` | Responsive headshot srcset used in `index.html` |
| `hs1.jpg`, `hs2.jpg`, `hs2-hires.jpg`, `hs3.jpg` | Additional headshot variants; `hs2-hires.jpg` is linked in bio section |
| `favicon-{16,32,64}.png` | Favicons |

### Sub-sections

#### *vgr: The Twitter Years (2007–22)*

Hosted externally at **https://books.venkateshrao.com/twitterbook/** — managed by the books project, not this repo. The `index.html` link points there directly. The old `/twitter-book/` directory has been removed from this repo.

#### `/BucketArt/` — Bucket Art gallery

Art project gallery. Appears to be a one-off section Venkat maintains separately.

| File | Purpose |
|------|---------|
| `index.html` | Main gallery page |
| `HowToMakeBucketArt.html` | Tutorial page |
| `images/full/` | Full-resolution gallery images |
| `images/thumbs/` | Thumbnail gallery images |
| `images.json` | Image metadata |
| `buckettool.webp`, `gridsandtextures.webp` | Tool/texture images |
| `index_old.html` | Old version (unused) |

#### `/stories/` — Fiction

Early-stage fiction section. `stories/index.html` currently has placeholder content ("test test test") — not polished/public yet.

| File | Purpose |
|------|---------|
| `index.html` | Auto-generated table of contents (has a generator script `index.py`) |
| `index.py` | Python script to regenerate `index.html` from story files |
| `story1.html`, `story3.html` | Individual stories |
| `story2/chapter1.html`, `story2/chapter2.html` | Multi-chapter story |
| `intro.html` | Introduction page |

#### `/pubdashboard/` — Publishing Dashboard

A dark-themed status dashboard tracking 41 publishing projects (Site/Ebook/Print status for each). **This file is generated externally** by `Publishing/Dashboard/build.py` in the `Code/Publishing/` project — do not edit `pubdashboard/index.html` directly. Commit the generated file here to deploy it.

## Styling Conventions

The main `index.html` uses minimal inline CSS in a `<style>` block — no external stylesheet. The design is intentionally plain and typographic. When editing `index.html`:
- Keep styles in the `<style>` block in `<head>`, not as inline `style=` attributes
- Max content width is 800px, centered with `margin: 3% auto`
- `#quotebox` is the blockquote style used for the Weick quote and bio blurb

## What's Deployed But Generated Elsewhere

| Path | Generated by |
|------|-------------|
| `library.html` | `vgr-library-code/` launchd agent (weekly) |
| `pubdashboard/index.html` | `Publishing/Dashboard/build.py` |

Do not hand-edit these files. To update them, run the relevant generator and commit the output.

## Deployment Notes

- No CI/CD — GitHub Pages deploys automatically on push to `master`
- No staging environment
- The custom domain is configured both in `CNAME` and in the GitHub repo settings
- Images are committed directly to the repo (no CDN or external asset store)
